// list of enums used in the app
// enum cannot be created inside a class

enum TextSizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  pending,
  processing,
  shipped,
  delivered,
  cancelled,
}

enum PaymentMethods {
  cashOnDelivery,
  paypal,
  esewa,
  khalti,
  imepay,
}
